#
# session settings for test use
#
# session使用数据库的DB信息
session.DbName=curve_mds_repo_test
session.DbUser=root
session.DbUrl=localhost
session.DbPassword=qwer
# session过期时间，单位us
session.leaseTime=100000
# 能够容忍的client和mds之间的时钟不同步的时间，单位us
session.toleranceTime=0
# session后台扫描线程扫描间隔时间，单位us
session.intevalTime=500000

#
# auth settings
#
auth.rootOwner=root
auth.rootPassword=root_password

#
# file lock setting
#
filelock.bucketNum=8